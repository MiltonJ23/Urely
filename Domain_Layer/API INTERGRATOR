# Group Leader, here i'll try to explain the code in details from the installation to the running process 

####################### INSTALLATION PROCESS ####################################################################################
if you haven't already installed Django REST Framework, you can do so using pip VIA  the command : pip install djangorestframework

Update settings.py
Add 'rest_framework' to your INSTALLED_APPS in settings.py:



################################### IMPLEMNTATION PROCESS ##########################################################################

 Create Serializers
Create a new file called serializers.py in your Django app directory 

Here is the code that defines the serializers :

from rest_framework import serializers
from django.contrib.auth.models import User
from .models import HealthLog, Doctor

class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ['id', 'username', 'email']  

class HealthLogSerializer(serializers.ModelSerializer):
    class Meta:
        model = HealthLog
        fields = ['id', 'user', 'date', 'log_type', 'value', 'notes']  

class DoctorSerializer(serializers.ModelSerializer):
    class Meta:
        model = Doctor
        fields = ['id', 'name', 'specialization', 'contact_info']  

#####################################################################


Now you have to create  Views for the API in the view.py :

from rest_framework import viewsets
from .models import HealthLog, Doctor
from .serializers import UserSerializer, HealthLogSerializer, DoctorSerializer

class UserViewSet(viewsets.ModelViewSet):
    queryset = User.objects.all()
    serializer_class = UserSerializer

class HealthLogViewSet(viewsets.ModelViewSet):
    queryset = HealthLog.objects.all()
    serializer_class = HealthLogSerializer

class DoctorViewSet(viewsets.ModelViewSet):
    queryset = Doctor.objects.all()
    serializer_class = DoctorSerializer


#please don't miss this part ooh , be focused when you reach this part 


############# Now Create a new file called api.py in your Django app and define it as such : ######################

from django.urls import path, include
from rest_framework.routers import DefaultRouter
from .views import UserViewSet, HealthLogViewSet, DoctorViewSet

router = DefaultRouter()
router.register(r'users', UserViewSet)
router.register(r'healthlogs', HealthLogViewSet)
router.register(r'doctors', DoctorViewSet)

urlpatterns = [
    path('api/', include(router.urls)),
]




####### THIS THE MOST TRIKY OR CONFUSED PART HERE #####################

DON'T FORGET TO Include the API URLs in your main urls.py  🧏🏽‍♂
######################################################################


THEN JUST RUN YOUR SERVER GRAND , OR YOU WANT ME TO INCLUDE IT 😂🙌🏾
